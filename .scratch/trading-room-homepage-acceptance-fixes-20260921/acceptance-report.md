# 首页验收修复交付记录

功能编号：trading-room-homepage-acceptance-fixes-20260921

当前状态：本轮6项修复及整体验收已完成。R1—R9独立审查发现均已修复并复核关闭；没有剩余范围内阻断项。完整测试仍有一项旧BOC fixture缺失，详见下方，不能称为“全套无错误”。

## 范围与数据保护

- 依据：上一轮PDF逐条复验失败项及本目录01—07任务；保持简约，不新增收益指标、数据源或重写导入算法。
- 工作区：`/Users/zhoulin/.codex/worktrees/ccc5/TradeReview`。
- 分支：`codex/review-remote-master-20260920`。保留开始前全部未提交改动；本轮不提交、推送、合并。
- 三名Luna实现者及文件边界见progress.md；协调者独立浏览器验收，另有只读代码审查。
- 有数据源：`.data/validation/layout-review.sqlite`，1857笔成交。通过SQLite `.backup` 创建一致性副本；浏览器写入只在副本发生。
- 验收数据库：`/tmp/tradereview-acceptance-20260921.nMxvK1/review.sqlite`。
- 源executions基线SHA256：`59d30c24be886fb241495c0fb5bdbb78a8a2e017e95327b51d0588898886e9cb`（按id排序完整行）。

## 预览及重启

当前验收预览：[交易室](http://127.0.0.1:3003/)。服务会话85299，使用当前工作树及上述独立数据库。原3002服务保持。

如服务停止，在确认3003端口空闲后执行：

```bash
cd /Users/zhoulin/.codex/worktrees/ccc5/TradeReview
TRADEREVIEW_DB_PATH=/tmp/tradereview-acceptance-20260921.nMxvK1/review.sqlite npm run dev -- --hostname 127.0.0.1 --port 3003
```

临时数据库可能被系统清理；若不存在，先重新使用SQLite一致性备份生成隔离库，不能静默启动空库或改用真实库进行写入验收。

项目流程：[development-workflow.md](../../docs/agents/development-workflow.md)。

## 已执行的独立浏览器检查

| 检查 | 已观察结果 |
| --- | --- |
| 1440 / 1280顶部控件 | 市场与期间标签顶部相同，控件顶部相同；不再出现原截图标签落在按钮下方的问题。 |
| 图表几何 | 桌面Y金额刻度对应位置误差0px，X锚点误差<0.02px；点约8×8px，按容器宽度生成viewBox。 |
| 320 / 390图表 | 绘图190px高，分别显示2/3个短日期刻度；点不变形；修正Y标签越过卡片边界的反例。 |
| 趋势明细 | 点击4月：期间+¥17,052.85、累计+¥87,661.20；Tab到5月：期间-¥55,941.17、累计+¥31,720.04；样本、胜负和日期齐全。悬停同处理器由行为测试覆盖，未声称浏览器单独执行无点击hover。 |
| 日历 | 全部年份→2026→2月→25日，完整显示+¥50,360.44、可信样本2、胜率50%。320日格25/盈/2笔，内外scrollWidth与clientWidth一致；390同样无格内溢出。月/年格保留金额和层级。 |
| 范围编辑 | 有效范围可应用；无效起日2028-01-01>结束2026-09-21显示错误且保留原范围；取消、快捷期间关闭编辑器。 |
| 筛选状态 | 日历内快捷期间、市场筛选保持日历；过期日期详情清除；月份下钻不弹编辑器。全部年份→快捷期间的金额一致性另被独立审查发现，修正中。 |
| 卡片详情 | “查看原币与汇率详情”可用Enter展开，显示完整原币小计、汇率来源与时间；主卡只保留短摘要。 |
| 持仓证据 | 四条负差额均显示方向/持仓证据待核对，保留负数量，成本与浮盈亏不冒充可信值；详情给出证据缺口。 |
| 行情实际请求 | 甘李药业出现重试进行中→报价¥68.91、浮盈亏-¥711.00、重试成功；不是仅检查按钮存在。 |
| 数据入口 | 稀有金属进入正确标的/账户的数据检查，看到三笔原成交；未修改、删除或补录成交。 |
| 原始数据 | 复核原layout-review库仍1857笔，摘要与基线一致；不是以孤立副本数量代替源库复核。 |

## 集中验证第一轮（保留失败记录）

- `npm run typecheck && npm run build`：退出0。构建有既有OpenCV浏览器externalization、大chunk及Node deprecation提示。
- `npx vitest run --maxWorkers=2 --reporter=dot`：退出1；201文件通过、4失败、2跳过，1876测试通过、3失败、5跳过，270.37s。
- 失败包含：旧BOC测试读取未跟踪且缺失的fixture；workspace旧aria-label；global错误详情断言匹配多个区域；图表测宽测试与运行中边距修改发生交叠。相关断言交负责人修正，保留原始失败，不调大默认超时或隐藏断言。
- 独立只读审查见independent-review.md。R1—R9尚待修复后逐条关闭；首次审查不是通过意见。

## 最终验证与交付

- 独立审查：R1—R9全部关闭；审查者另验证54组期间×层级×分桶组合及真实组件切换流程，见independent-review.md。
- 中央定向：7文件86项通过（摘要、质量UI、持仓、行情retry、workspace refresh）；图表/日历3文件36项由最终完整套件再次覆盖。
- 完整命令：`npx vitest run --maxWorkers=3 --reporter=json --outputFile=.scratch/trading-room-homepage-acceptance-fixes-20260921/vitest-final.json`。1898项通过、5项跳过、0失败断言；208文件中仅`app/lib/fx/boc-parser.test.ts`初始化失败，原因是旧测试所引用的未跟踪文件`.scratch/trading-room-implementation/reports/boc-source.html`不存在。原测试未改；没有删除/跳过该失败套件，也没有编造fixture。命令退出1，原始结果保存于vitest-final.json。
- 最终`npm run typecheck && npm run build && git diff --check`退出0；保留既有Node deprecation、OpenCV browser externalization和大chunk构建警告。
- 浏览器最终范围：全部年份695可信回合→YTD62/+¥29,146.06（月格）→全部年份695→本月0（日格）→近3月4（三个月格）；自定义2025年116/+¥126,291.22，随后仍能恢复全部年份695。顶部摘要、累计末值和点明细范围一致。
- 日历前后翻页成功；2月25日打开NFLX对应回合，返回交易室仍是日历并保留日期详情；未保存/修改成交。
- 实际行情：除甘李药业首页重试成功外，SNDK数据管理重试出现进行中后变为可用估值，持仓可用数从1增至2。失败/未启动/日线成功而小时线失败/存储错误由行为测试与独立重放验证，未假装外部源失败已全部恢复。
- 诊断一致性：黄金ETF和半导体在首页及质量页都以“行情早于最近交易”为主因，源失败/过期为次级状态；四个待核对负仓不进入行情修复可解决的误导路径。
- 最后浏览器检查：2026-09-21 21:43 CST，3003当前分支正常，“我的交易室”/今年至今/月趋势图存在；13:25:43 UTC后未见新增error/warn。原3002重新加载也有新详情入口及4条待核对标记。开发中间态曾有一次已修复的旧helper错误，未隐去历史日志。
- 最终数据复核：源layout-review仍1857笔，SHA256与开工基线相同；源tradereview仍0笔；隔离库仍1857笔。行情/复盘导航验收没有改变真实成交库。
- 服务仍运行：3003 PID86207、会话85299，cwd即当前ccc5/TradeReview；3002 PID82182保持。最终预览3003已真实浏览器打开并保留为交付页，默认打开代码仍为实盘/本月，当前验收页停在今年至今/月趋势方便review。
- 无提交、推送、合并；已有其他未提交工作保留。启动/重启方式及项目workflow链接见上方。

## 本轮边界

- 四条负仓仅能证明导入流水净差额，不能证明真实账户空头或具体漏单；不自动修改成交、补数或改正负号。
- 行情恢复依赖现有外部行情源，必须报告实际终态；请求完成不等于报价可用于估值。
- PDF提到的赔率、夏普率属于此前已明确排除的新计算口径，不将其算作已实现。
