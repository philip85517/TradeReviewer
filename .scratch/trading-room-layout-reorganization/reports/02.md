# Ticket 02 实现报告

## 需求/验收清单

- [x] 按标的 / 按回合标签移至 `libraryHeader` 下方顶层；主列表共用同一组标签，详情不重复显示。
- [x] 标签统一使用 `module-tabs`，保留 `role="tab"`、`aria-pressed` 和 `aria-selected`。
- [x] 标签支持鼠标切换及 `ArrowLeft`、`ArrowRight`、`Home`、`End` 键盘选择，并在视图切换后保持焦点。
- [x] 切换浏览方式时保留已有筛选状态。
- [x] 空库支持可选 `onImport?: () => void`，提供“去导入”动作；文案改为“使用下方‘去导入’添加券商成交记录”。
- [x] 标的列表和回合列表在筛选无结果时提供“清除筛选”；空库在提供 `onImport` 时提供导入动作。
- [x] `entries.length === 0` 时两种浏览方式均只渲染 header、tabs、单句空态和可选“去导入”，不渲染筛选、汇率或绩效区。
- [x] 精简交易库 header 旧说明。

## 修改文件

- `app/components/library/trade-library.tsx`
- `app/components/library/trade-library.test.tsx`
- `.scratch/trading-room-layout-reorganization/reports/02.md`

未修改 `workspace`、`app/globals.css` 或其他组件；旧 `library-view-tabs` class 已从组件移除，仅保留统一 `module-tabs`。

## 测试与证据

先补充行为测试并确认红灯，覆盖标签位置/状态保持、详情不重复、ARIA、键盘导航、空库导入、标的无结果和回合无结果动作；随后完成最小实现并复验。

命令：

```bash
npx vitest run app/components/library/trade-library.test.tsx app/components/library/trade-library.acceptance.test.tsx --testTimeout=15000
```

结果：2 个测试文件、40 个测试全部通过。

使用项目默认 5 秒单测超时复跑时，分页 acceptance 用例曾超时；未改测试配置，使用显式 15 秒命令复验通过。该用例耗时约 6.85 秒。

按用户要求未运行全量测试、构建或浏览器验收；本次浏览器反馈对应的空库结构已由相关组件测试覆盖。

## 未完成项/限制

主 workspace 的空库挂载条件和 `onImport` 接线仍由协调者负责；本 ticket 未触碰该文件。
