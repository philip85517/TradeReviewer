# Recall 基线合并到 master

用户授权将 `feature/trade-reviewer-mvp` 合并到远端 `master`，并将仓库默认分支设为 `master`。

- 初始 master：`dd59a89d39a3a9da90fb99f35dff078dfeb8ac75`。
- 验证期间 master 更新至 `452cda87249a53a11c064530dfa4d3a7e213c65f`（交易室首页），已再次合入，未出现新增文本冲突。
- 合入基线：`38b644a66dfc662f3a0d2b5ff98c9cae6b01131d`（Recall PR #18）。
- 使用独立集成分支 `codex/recall-master-integration`；不改动其它工作树。
- Luna Max 分别处理工作台和图表/存储冲突，独立测试任务只接收合并范围与检查要求。

## 合并原则

保留 master 的交易库、首页、人民币统计、模拟盘和导入更新，同时接入 Recall 图上记录、快照、回合完成及导出。数据库保持 master 已有迁移的顺序和校验和，将 Recall 表作为后续迁移追加。

## 合并发现与处理

- master 的迁移 5（模拟盘范围）和 6（中文名称）保持不变，Recall 文档新增为迁移 7。
- 图表保留 master 的定位、聚合成交标记和视野行为，同时保留 Recall 的同步截图刷新、图文合成和历史快照能力。
- 工作台切换到 Recall 后需补回已有的币种/费用与盈亏可用性提示、模拟盘来源说明，避免因入口替换丢失原有保护。
- 侧边笔记表单由图上 Text 替代是已确认规格；不将其作为合并遗漏恢复。

## 验证

最终完整单元/交互测试：220 个文件，1905 项通过，0 失败，5 项原有样本依赖测试跳过。类型检查通过；ESLint 为 0 错误、15 个警告；构建及 5 项页面/SQLite 运行时检查通过。主工作台 71 项、导入流程 29 项及刷新/日期/汇率修复的 13 项均已通过定向检查。

测试适配保留 master 功能覆盖，按 Recall 现有控制和仓库保存行为替换旧侧边笔记断言。BOC HTML 测试文件移入正式 fixtures 目录；日期相关测试固定时间，不再随执行日期变化。

本地目录选择器实际写入的既有验收限制仍适用，见 [Recall 验收记录](2026-09-19-recall-workspace.md)。

## 后续检查

独立复查另记录了两条原有历史模式导航的代码审查发现，尚未进行浏览器复现或在本次合并中修复，见 [后续记录](2026-09-20-recall-followups.md)。这不属于本次冲突引入的改动，不能把合并通过测试解释为这两条路径已验收。
