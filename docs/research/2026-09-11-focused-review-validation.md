# 精简复盘实现与验证

已完成三问复盘、回合队列、当前回合图表聚焦、账务/路径指标分离、行动追踪和阶段总结。

## 使用路径

- 交易库默认显示待复盘队列，支持按账户、年份等筛选；完成保存后进入同一范围的下一回合，暂缓需记录原因。
- 详情页并排展示图表和三问编辑器；成交明细、原始计划和标签折叠，原笔记保留。
- 下次行动可选择追踪，后续同范围回合可记录遵守、偏离或不适用；历史检查保存规则快照。
- 模式洞察入口显示阶段总结。账户、市场、交易性质、模拟运行、币种和日期范围一致；基础结果不依赖行情，路径指标缺失单独展示。
- 总结中的保持、修正、下期重点和证据写入 SQLite。未保存草稿和日期范围在查看证据并返回时保留。

## 验证

- 最终全量：149 个测试文件通过、2 个跳过；1331 项通过、3 项跳过。
- 最后慢保存并发修复后：总结和 API 专项 8 项通过，其中新回归先复现失败再修复。
- 类型检查、构建通过；ESLint 无错误，3 条原有 warning。
- 独立复审发现的问题均已修复并通过复审，包括保存冲突、账户范围、重新打开回合、图表周期切换及总结保存竞态。
- 网页验证完成：三问保存后继续、刷新恢复、日/周线聚焦、规则检查、停用规则后历史保留、阶段总结保存恢复与证据导航草稿保留。
- 本地测试地址 http://localhost:3117/，使用独立复制数据库 `.data/focused-review-qa/review.sqlite`，不占用其他服务端口。测试笔记只写入复制库。
- 331 笔原始成交校验不变：SHA256 `6a7ec767bf16c2ba607bbc03c04bd81d1a2deb3273eb15d7d52b48fa071de138`。

所有变更已在本地分支提交；本次未推送远端或合并。
