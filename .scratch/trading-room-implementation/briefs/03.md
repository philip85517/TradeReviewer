# 03 最新汇率实施（前置01/02验收通过后派发）

完整读取spec、issues/03-fx.md、reports/01-contract.md、reports/02.md及reports/fx-source-evidence.md。
权限：新增app/lib/fx/*、app/api/fx/*、app/components/data-management/fx-panel*、其tests；共享SQLite settings/store/client改动需协调者明确分配。禁止改原交易持久化逻辑、dashboard装配及workspace，提供组件和hook由其负责人接线。

产出类型化FxState与hook，消费01金额契约。采用BOC中行折算价（每100单位转每1单位），保存source/publishedAt/fetchedAt/rates/lastAttemptDay/status/error；用户输入无敏感数据向源发送。最新快照专用namespace，不创建历史库、不覆写ChartSettings。每天Asia/Shanghai日期第一次打开自动尝试一次含失败，显式manual可重试；多页/并发请求服务端去重，持久尝试日期，严格超时/有限重试。手动更新成功可连续两次，确保no-store、同屏唯一快照原子替换；失败留旧值或无值状态。UI在数据管理提供来源、更新时间、币种率与刷新操作。

先TDD明确解析含单位/坏HTML/缺币种、持久化、日调度、失败回退、并发；真实源访问证据与mock必须分别记录。解析不可把空白/未知写成0，异常不同日期不拼成伪造完整最新快照，原币fallback。
读取共享sqlite store可复用app_settings；需要改共享契约先提明确最小接口。报告reports/03.md，范围与红绿/真实GET/命令/限制。
