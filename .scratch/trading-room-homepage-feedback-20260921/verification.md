# 首页反馈修复验收记录

> 后续更正（2026-09-21）：以下为首轮执行记录，不代表最终通过。对原 PDF 的逐条复验发现图表非等比拉伸/刻度错位、窄屏月历内部溢出、卡片与持仓文本密集、负持仓判断未闭环等问题。最新结论以 [acceptance-review.md](acceptance-review.md) 为准，功能状态已重新打开。整页无横向滚动不能证明控件内部无遮挡。

日期：2026-09-21  
工作树：`/Users/zhoulin/.codex/worktrees/ccc5/TradeReview`  
分支：`codex/review-remote-master-20260920`

## 交付预览

服务已启动并保持运行：

`http://127.0.0.1:3002/`

启动命令：

```bash
TRADEREVIEW_DB_PATH=/tmp/tradereview-feedback-20260921.sqlite npm run dev -- --hostname 127.0.0.1 --port 3002
```

本轮浏览器验收使用隔离数据库 `/tmp/tradereview-feedback-20260921.sqlite`，未使用原始校验库写入。

## 代码与自动化验证

- 相关组件/模型测试：70/70 通过。
- 最终 `room-performance` 聚焦测试：11/11 通过。
- `npm run typecheck`：通过。
- `npm run build`：通过。
- `git diff --check`：通过。

构建仅有既存工具链提示：部分 Node 内置模块被 externalize、存在大于 500 kB 的 bundle，以及 vinext 路由静态分析提示；没有构建失败。

## 真实浏览器验收

- 桌面宽度 1280：市场分类与统计期间控件顶部对齐误差为 0px；页面 `scrollWidth === clientWidth`。
- 窄屏宽度 390：范围卡片在视口内，无横向溢出。
- 窄屏宽度 320：范围控件和趋势图仍在视口内，无横向溢出；趋势横轴收敛为代表性刻度，避免标签拥挤。
- 顶部范围控制：实盘/模拟盘切换、当前持仓入口、市场分类筛选均可见；默认使用实盘范围。
- 更多期间：点击后出现开始日期、结束日期、应用期间、取消控件；无效期间由现有校验反馈并保留当前范围。
- 趋势：趋势点可聚焦/点击，显示期间收益、累计收益、样本、胜负/持平与日期覆盖详情。
- 日历：显示金额、样本及可可靠计算的胜率；分母无效时显示“胜率不可用”。
- 当前持仓：显示多头/空头/方向待核对，并对缺失、过期、未来、交易前、币种不匹配、价格无效等行情状态给出诊断和现有数据动作。
- 浏览器 error/warn 日志：空。

## 数据安全核对

原始库与隔离库的关键记录数一致：

| 数据表 | 原始库 | 隔离库 |
| --- | ---: | ---: |
| `executions` | 1857 | 1857 |
| `instruments` | 236 | 236 |

本轮只保留工作区已有改动并新增本功能代码与记录，未提交、未推送、未合并。
