# 04 — 数据管理内容分组报告

## 实现

- `DataManagement` 新增可选受控接口：`activeTab?: 'import' | 'quality' | 'settings'` 与 `onTabChange?`；未受控时默认 `import`。
- 顶部使用现有 `.module-tabs` 和 `aria-pressed`，标签为“数据接入 / 数据质量 / 收益配置”。
- 数据接入保留导入、行情更新和导入记录入口；数据质量承载质量 slot 与待检查问题；收益配置承载本金和汇率 slot。
- 非当前分组使用显式 `style.display = 'none'`，slot 保持挂载以保留草稿；缺失 slot 显示简洁空态。
- 精简数据管理 header 说明；未新增 history 页、路由、数据或业务写入。

## 修改路径

- `app/components/data-management/data-management.tsx`
- `app/components/data-management/data-management.test.tsx`
- `.scratch/trading-room-layout-reorganization/reports/04.md`

未修改 `app/globals.css`。

## 浏览器可读性最小修正

最终浏览器验收发现质量明细白底卡片继承应用深色文字，导致维度标题和主数量对比度不足。补充 `app/components/data-management/quality-details.module.css` 的 `.dimension`：

```css
color: var(--dashboard-ink, #17212b);
```

该修正仅作用于质量明细白底卡片，不改变业务逻辑、数据、路由或全站主题；按要求未新增测试。

## 验证

命令：

```bash
npx vitest run app/components/data-management/data-management.test.tsx
```

结果：`1` 个测试文件通过，`9/9` 个测试通过。

覆盖：默认标签、受控/非受控切换、可见性、业务 callback 保留、隐藏 slot 草稿保留、空 slot 空态及原有导入/行情/质量/本金/汇率行为。

未运行全量测试、全量构建或全量 typecheck，遵循本任务“只跑对应测试”的要求。
