# 首页与复盘易用性改进验收记录

日期：2026-09-06。基线：`f2361fb`。范围：任务 01–09；当前隔离工作区内的本地实现。

## 交付行为

| 任务 | 实现与验证 |
| --- | --- |
| 01 窄屏入口 | 全局共享导入入口、单实例股票抽屉；390px 实测打开后焦点进入关闭按钮、背景 inert、Escape 关闭并返回触发按钮，无横向溢出。 |
| 02 回合定位 | 使用股票与回合稳定身份跳转，保留目标状态；多账户历史回合及失效目标有回归。浏览器实测华虹宏力第 1 次交易（富途）进入复盘仍为该回合，未跳到第 4 次交易（老虎）。 |
| 03 空首页 | 中央文件导入和次级截图恢复、支持格式说明与确认前不入库说明；独立空 SQLite 数据库实测桌面和手机空态。加载/存储失败仍由独立状态界面处理。 |
| 04 股票查找 | 名称/代码搜索、最近成交稳定排序、待复盘筛选、无结果清除；81 只股票的交互回归与真实数据副本验证，筛选不请求行情。 |
| 05 回放主操作 | 回合日期/账户/复盘状态、文字开始/继续/暂停、下一成交与终点提示；低频绘图及批量维护收进可展开入口。安全起点不再因历史缺失跳到未来首根 K 线。 |
| 06 分阶段笔记 | 单列表单、独立展开计划/总结，保留自动保存、失败重试及知识时点约束；展开总结后连续输入不会折叠或丢失计划。仅闭合回合走到可用终点时优先展开总结，持仓中回合不会仅因缓存用尽而切换阶段。 |
| 07 交易库操作 | 股票库/详情共享导入入口，详情可更新当前股票行情；取消和确认导入、行情失败后仍保留原回合与搜索条件。更新中禁用重复提交，部分可用/失败状态有明确文字。 |
| 08 浏览上下文 | 页面会话保留筛选、股票、回合及滚动位置；集成测试验证搜索、市场/年份、scrollTop=240 的往返恢复。相同路径额外本地行情读取由 18 次降为 0 次。 |
| 09 可读性 | 原有 8–10px 必要文字提高到至少 12px，表单常用文字 14px；关键持仓指标优先显示，其余可展开。无行情统计原因改为一次中文提示。1280、1024、390px 页面检查已完成。 |

## 自动验证

- `npm run test:unit -- --maxWorkers=2`：110 个测试文件、948 项测试全部通过。
- `npm test`：生产构建成功；4 项渲染、SQLite 运行及客户端数据边界验证通过。
- `npm run typecheck`：通过。
- `npm run lint`：0 个错误；保留已有 `storage-boundary.test.tsx` 未使用变量警告 1 个。
- `git diff --check`：通过。

## 页面与数据边界

- 预览使用独立交易数据副本和独立空数据库；不修改用户原始交易数据库，不发布到远程站点。
- 桌面主界面、1024px 复盘/交易库、390px 导入/股票抽屉/交易库已目视检查。390px 文档 scrollWidth 等于视口宽度。
- 关键颜色样本：主按钮白字 / `#2169cc` 对比度 5.31:1；辅助文字 `#a5b6cd` / `#182333` 为 7.67:1；盈亏文字保留正负号。
- 原生浏览器缩放快捷键在内置浏览器中未生效；使用 640×360 CSS 视口验证相当于 1280×720 下 200% 放大的重排与操作可达性。这不等同于完整的原生缩放实测。
- 导入和行情异常链路通过自动交互测试验证，未为验收向真实行情服务重复下载数据。

## 状态

任务 01–09 已实现并完成上述验证。原生浏览器缩放实测是本次工具环境的验证边界，已在任务 09 同步注明。当前改动保留在本地工作区供审阅。
