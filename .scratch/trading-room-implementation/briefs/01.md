# 01 开发派发

先完整读spec、docs/specs/2026-09-19-trading-room-domain.md、issues/01-scope.md与AGENTS.md。用户已批准执行。你为gpt-5.6-luna/max，负责01垂直功能，最终也持有dashboard装配权。

允许改：app/components/dashboard/review-dashboard.tsx及同名CSS/tests；新增 app/lib/reviews/trading-room*.ts及tests、新增dashboard目录专用scope组件；app/lib/trades/library.ts及其tests（仅添加可选投影字段且兼容现有调用）。禁止改workspace、globals.css、settings/store/API、其余模块。需要额外文件先报告协调者。

要求：原有可信统计可复用，避免重写财务算法。资产类型从metadata投影与有据分类获得，未知显式排除/提示；字段需可接收从workspace传入的metadata映射（请先定义prop并通知协调者，由02接线），不改交易Instrument身份。先约定类型化RoomScope、RoomMoneyView/FxSnapshot输入（可无快照）。01交付有完整范围和基础摘要的可运行UI，其他待开发模块留清晰组件插槽而非伪造功能。不移除已有必要功能但新UI不需保留重复统计组卡。

输出：统一分类、自然月日期/模拟运行选择、高级筛选、基础同范围摘要；onOpenInReview签名保持兼容。后续04/05/06组件通过小模块集成，先导出明确model、rows与scope契约，写入 reports/01-contract.md。持仓需要忽略业绩日期的scope过滤方法，按需提供。

步骤：等待协调者通知baseline完成后才改产品文件；此前可读取和设计测试。TDD红→绿，定向测试不要跑全量。npm ci协调者负责。不要git commit（共享工作树）；不要启动额外服务、写正式数据库、推送/合并、再派子代理。修改仅属你文件。
报告 reports/01.md 包含事实核查、文件、红绿证据、命令/结果、契约、限制。每阶段发简短进度，遇未定技术常规选择做最小正确实现并写依据；关键接口与协调者对齐。
