# 01 — 左侧导航与交易室布局交接报告

## 需求/验收清单

- 四个一级入口统一由左侧主导航承载：我的交易室、交易库、模式洞察、数据管理。
- 当前入口使用 `aria-current="page"`、选中背景和侧边指示线表达。
- 窄屏复用同一份导航，通过“导航”按钮展开/收起；选择入口后自动收起。
- 交易室、交易库、模式洞察、数据管理直接复用现有业务标题，不新增重复 page title。
- 复盘页保留必要操作；演示态离开复盘后保留独立的“返回演示复盘”操作。
- 未改数据模型、财务计算、数据库、复盘业务内容及 02–04 模块内 tabs。

## 改动文件

- `app/components/trade-review-workspace.tsx`
  - 将一级导航移入侧栏语义容器。
  - 增加窄屏导航开关状态，复用同一组入口与当前态。
  - 复盘页保留必要操作栏；非复盘业务页不增加重复标题栏；演示返回单独保留。
- `app/globals.css`
  - 桌面侧栏宽度固定为 190px，主内容使用剩余空间。
  - 窄屏切换为顶部品牌 + 导航按钮 + 展开菜单，避免整体横向溢出。
  - 提供后续 02–04 共用的 `.module-tabs` 基础样式：下边线、选中态、`aria-pressed` 语义和局部横向滚动；未新增抽象组件。
- `app/components/trade-review-workspace.test.tsx`
  - 新增导航行为测试，覆盖共享入口、当前项、窄屏菜单开关和键盘焦点。

## 验证结果

- 新增 navigation 行为测试：1 passed（由协调者独立复验）。
- 相关复盘导航回归：3 passed。
- workspace refresh 相关测试：7 passed。
- `npx tsc --noEmit`：通过。
- `git diff --check`：通过。
- 协调者独立浏览器验收：1280px、390px 均通过；导航默认隐藏、菜单切换、单一业务 h1、无整体溢出通过。

## 后续共用样式约定

02–04 直接在内容顶部使用 `.module-tabs`，按钮沿用 `aria-pressed`；如采用 `role="tab"`，需同时补齐对应键盘行为和选中语义。该任务不提供 tabs 抽象组件，也未提前引入业务模块状态或回调。

## 阻塞与交接

无阻塞。未提交、未推送、未启动或修改数据库服务。可由 02–04 继续接入受控状态、回调和各模块 tabs。
